driver=chrome
timeout.wait.element=10
domain=https://jdi-framework.github.io/tests/

#it doesn't work Error is: "Unknown driver: ${driver}"
#driver=${driver}
driver.getLatest=true
drivers.folder=src/main/resources/driver/
log.message.format=short
multithread=true
screenshot.strategy=on fail
